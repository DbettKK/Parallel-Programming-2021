三组样例产生式数量均为最大值，但是字符串长度和数据分布不同。
第一组是稠密小样例，即语法树的每棵子树的孩子都比较多，但字符串长度小；
第二组是稀疏大样例，即语法树的每棵子树的孩子都比较少，但字符串长度大；
第三组是不均衡大样例，易产生负载不均问题，并且字符串长度大。
前两组TLE时间比串行代码运行时间长一点，防止很多人得0分，但不可直接交原始代码。
第三组样例用时可能很长，为避免浪费助教的时间和学校超算平台的机时，第三组的TLE时间比串行代码短，需要进行有效优化才能通过。
前两组样例中若任意一组出现TLE，则第三组直接视为TLE。
最终成绩按排名计算，由老师给分。